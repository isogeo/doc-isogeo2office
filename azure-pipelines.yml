trigger:
  batch: true
  branches:
    include:
    - master
  tags:
    include:
    - "*"

pr:
- master

jobs:
- job: 'BUILD'
  pool:
    vmImage: "windows-2019"
  
  steps:
  - task: NodeTool@0 
    inputs:
      versionSpec: '10.15.x'

  - script: yarn
